local reverb = {}; // this is local module like.
local strengh = 1; // this is a reverb Amp.
local time = 200; // this is a amount of reverb.
local duld = 30; // this is a wet.
local chid = OnEvery(); 
local function str()
	link strengh "Slide";
	state change "lib.os.sound.SoundEngine.Reverb.""strengh","amount" to {linker}:
	end
OnEvery()
	sin;
	end
